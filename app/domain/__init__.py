"""Provider-neutral domain models.

Nothing in this package knows that Gemini, Ollama or Angel One exist. These are
the types the backend sends, the types the service reasons about, and the types
it returns.
"""
